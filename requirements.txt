# =============================================================================
# SKYLEX — Production Dependencies (May 2026)
# Fully Compatible & Stable Versions
# =============================================================================

# Environment & Configuration
python-dotenv>=1.0.0,<2.0.0        # Environment variables management (.env file support)

# Numerical Computing
numpy>=2.0.0,<3.0.0                # Core numerical operations and array support

# =============================================================================
# LangChain Ecosystem (v1.x - Stable)
# =============================================================================
langchain>=1.2.0,<2.0.0            # Main LangChain framework for agentic workflows
langchain-community>=0.4.0,<1.0.0  # Community integrations and third-party tools
langchain-openai>=1.2.0,<2.0.0     # OpenAI integration (Chat, Embeddings, etc.)

# OpenAI SDK
openai>=2.26.0,<3.0.0              # Official OpenAI Python client (required by langchain-openai)

# Observability & Monitoring
langsmith>=0.7.0,<1.0.0            # Tracing, debugging, and monitoring with LangSmith

# Experiment Tracking & ML Lifecycle
mlflow>=2.20.0,<4.0.0              # MLflow for experiment tracking and model management

# =============================================================================
# API Layer
# =============================================================================
fastapi>=0.110.0,<0.140.0          # FastAPI framework for building high-performance APIs
uvicorn>=0.30.0,<0.40.0            # ASGI server to run FastAPI applications

# Data Validation & Settings
pydantic>=2.0.0,<3.0.0             # Data validation using Python type hints
pydantic-settings>=2.0.0,<3.0.0    # Settings management using Pydantic v2

# HTTP Client
requests>=2.32.0,<3.0.0            # Standard HTTP requests library

# HTTP Client (async-capable, better than requests)
httpx>=0.27.0,<1.0.0               # Async HTTP client for high-concurrency API orchestration

# Progress bar for ingestion loops
tqdm>=4.66.0,<5.0.0                # Console progress bars for tracking parsing loops

# PDF text extraction
pdfplumber>=0.11.0,<1.0.0          # High-precision PDF text and structural table extraction

# HTML scraping
beautifulsoup4>=4.12.0,<5.0.0      # HTML/XML parsing for SkyBrary web scraping pipelines

# Experimental LangChain features (SemanticChunker etc.)
langchain-experimental>=0.4.0,<1.0.0  # Semantic chunking and embedding-bound splitters

# Data visualization — chunk size distribution plots for MLflow artifacts
matplotlib>=3.9.0,<4.0.0           # Histogram plots for chunking experiment artifacts

# Retry logic with exponential backoff + jitter for OpenAI API rate limits
tenacity>=9.0.0,<10.0.0             # Production-grade retry for embedding/LLM calls

# =============================================================================
# Vector Databases & Retrieval Lifecycle (v2 Additions)
# =============================================================================
chromadb==0.6.3                    # Local persistent vector database for testing
sentence-transformers==3.4.1       # Local embeddings computation and encoding models
ragas==0.2.14                      # Evaluation framework for RAG pipeline validation
datasets==3.5.1                    # Hugging Face datasets container for RAG evaluation sets
qdrant-client==1.14.2              # Production-grade vector store client for remote cluster indexing
rank-bm25==0.2.2                   # Traditional lexical search scoring function for hybrid retrieval
FlagEmbedding==1.3.4               # BGE reranking models interface for search reranking optimization